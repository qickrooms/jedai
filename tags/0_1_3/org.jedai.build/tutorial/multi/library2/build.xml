<project name="library2" default="build">
    <import file="build-imports.xml" />

    <path id="flex.lib.path">
        <pathelement location="${multi.library1.swc}" />
    </path>

    <!-- targets -->
    <target name="build" depends="init,flex-library" description="Build the library" />
</project>
