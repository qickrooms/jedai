package com.infrared5.io
{
	public class DeviceAutoConfig
	{
		/**
		 * CONFIG_DIRECTION_TOKEN is used to define which item in a two-way negotiated
		 * connection started the connection so we don't end in a loop.
		 */
		public static const CONFIG_DIRECTION_TOKEN	: uint = 0xff0;
	}
}